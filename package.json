{
  "name": "calc-ts",
  "version": "0.1.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist/ && tsc",
    "test": "tsc --noEmit",
    "prepare": "yarn build"
  },
  "devDependencies": {
    "prettier": "2.2.1",
    "typescript": "4.1.3"
  }
}
